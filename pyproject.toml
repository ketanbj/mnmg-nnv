[project]
name = "mnmg-nnv"
version = "0.1.0"
description = "Ray cluster experiment for Docker Compose and Slurm"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
  "ray==2.53.0",
]

[tool.uv]
package = false
